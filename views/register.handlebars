<main class="container">
  <div class="center-content">
    <div class="col-md-10">
      <div class="register-form-card">
        <div class="register-body-header">
          <h1>Register</h1>
        </div>

        <div class="register-body-main">
          <!-- Opening the form tag -->
          <form id="register-form">
            <div class="register-body-content">
              <!-- Username input field -->
              <div class="formRow">
                <label class="formRow-label" for="username">Username:</label>
                <div class="formRow-input">
                  <input
                    type="text"
                    id="username"
                    name="username"
                    placeholder="Enter Username"
                  />
                </div>
              </div>

              <!-- Email input field -->
              <div class="formRow">
                <label class="formRow-label" for="email">Email:</label>
                <div class="formRow-input">
                  <input
                    type="email"
                    id="email"
                    name="email"
                    placeholder="Enter Email Address"
                  />
                </div>
              </div>

              <!-- Password input field -->
              <div class="formRow">
                <label class="formRow-label" for="password">Password:</label>
                <div class="formRow-input">
                  <input
                    type="password"
                    id="password"
                    name="password"
                    placeholder="Enter 8 Character Password"
                  />
                </div>
              </div>

              <!-- Register button and login message container -->
              <div class="button-and-message-container">
                <!-- Login message with a link to the login page -->
                <p class="login-message">Already have an account?
                  <a href="/login">Login</a>
                </p>

                <!-- Submit button -->
                <button class="register-btn" type="submit">Register</button>
              </div>
            </div>
          </form>
          <!-- Closing the form tag -->
        </div>
      </div>
    </div>
  </div>
  <!-- Include JavaScript file for registration logic -->
  <script src="/js/register.js"></script>
</main>